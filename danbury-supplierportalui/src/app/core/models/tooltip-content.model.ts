/**interface for TooltipContent to display in tooltip overlay */
export interface TooltipContent {
    /** Prop for key to findout right question and value */
    key: string;
    /** Prop for value to display in tooltip content */
    value: string;
}
