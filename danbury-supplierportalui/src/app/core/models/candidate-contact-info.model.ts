/** interface for candidate contact info */
export interface CandidateContactInfo {
    /**member variable for candidateName */
    candidateName: string;
    /**member variable for phoneNumber */
    phoneNumber: number;
    /**member variable for emailId */
    emailId: string;
}
